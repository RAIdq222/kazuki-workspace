Pillow>=10
numpy>=1.24
# 作業コンソール(genzu_fix.server)とパース編集エディタ(genzu_fix.perspective_editor)。
Flask>=3
psd-tools>=1.17
# 絵コンテ(PDF)→ページ画像化（genzu_fix.conte render）。Vision抽出は標準ライブラリで
# Anthropic REST を叩くため追加依存なし（要 ANTHROPIC_API_KEY）。
PyMuPDF>=1.24
