# 背景原図 修正支援ツール — 設計メモ（ドラフト）

> ステータス: 要件整理〜PoC段階。レビュー必須。精度検証は別セッションで実施予定。

## 1. 目的

商業アニメの背景原図（美術への依頼用ラフ）を、美術ボードを参照しつつ、
指示内容（建物・自然物の配置、キャラ位置）を制約として守ったまま、
パースの狂いや設定との齟齬を画像生成で修正する。

- 生成物は**最終納品ではなく、美術監督が確認・修正する叩き台**（ただし品質は納品レベルを志向）。
- 1話あたり約300カットを一括処理したい。
- 入力PNGは **PSD原図からの自動書き出しに対応**（§10、`src/genzu_fix/psd_export.py`）。
  レイヤー構造は不定形だが、保存済みの「表示状態」をそのまま合成すればよいので自動化できる。

## 2. 使用モデル / 確認済みの事実

- 画像生成: **Higgsfield 経由の GPT Image 2**（model_id: `gpt_image_2`）。
- 確認済みスキーマ:
  - 画像入力（編集）対応。tags に `editing` あり。
  - 解像度ティア: `1k` / `2k` / `4k`（デフォルト 1k）。
  - 品質: `low` / `medium` / `high`。
  - 比率（離散・整数比のみ）: `1:1, 4:3, 3:4, 16:9, 9:16, 3:2, 2:3`。
- 未確定（要・お試し検証）:
  - 入力画像を**2枚同時**（原図＋美術ボード）渡せるか。不可なら美術ボードは言葉で渡す。
  - 「配置を保ったままパースを直せるか」の実効性能。

## 3. MVP スコープ

「**指示プロンプト作成 ＋ 生成実行**」まで。検証ループ・PSD格納は後段。

```
原図PNG  ─┐
          ├─► ① 原図理解(GPTで構図/配置/指示書きを把握。指示文字・補助線は無視)
香盤表    ─┤
美術ボード ┘
              ▼
          ② プロンプト統合（場面設定 + 守る制約 + 直す指示 + 画風参照）
              ▼
          ③ 比率・解像度の事前調整（余白パディングで許容比率へ）  ← 実装済み
              ▼
          ④ GPT Image 2 で生成（Higgsfield）
              ▼
          ⑤ 余白を切り戻して元画角に復元 → 保存
```

## 4. 香盤表（Excel）について

列構成: `カットNO / 時間 / 場所 / 3Dモデル / 天気 / 光源 / 色味 / キャラ / 服装 / 小物 / 2D / 3D / 備考`。
セクション: `アバンpart / OP / Apart / CM / Bpart`。

- 場面設定（時間・天気・光源・色味・場所）はこの表から機械的に取得できる。
- カットNO は `3～14`（レンジ）, `2、3`（リスト）, `欠番`(備考), `回想`注記が混在
  → カット番号→行の解決ロジックが必要（レンジ展開・欠番除外）。
- 場所名（例 `南康郡_街`, `住宅街(隔離空間)`）と美術ボードのファイル名は厳密に紐づかない
  → 「場所名 → ボードファイル」対応表を作品ごとに1回作る（半手動）。
- 原図の欄外にも `BG:場所名` が書かれており、香盤表との突合の手がかりになる。

## 5. 原図理解で見るべき観点（v1 仮説 / 育てる対象）

実物（南康郡の街並み／一点透視）から判明した重要点:
**原図には「絵そのもの」以外に、指示・補助の描き込みが乗っている。**
（手書き指示 `淡色処理`、欄外管理情報、パース補助線・VPマーク・フレーム枠・タップ穴）
→ 「直す対象の絵」と「指示・補助線」を**分けて扱う**工程が必須。

観点（初期案）:
- 構図種別（一点/二点透視 等）、消失点・地平線の位置、画角
- 主要構成物の**配置**（左右・奥行き・前後関係）= 守るべき制約
- キャラの立ち位置（描かれる場合）= 守るべき制約
- パースの収束ズレ、様式的な正しさ（建築様式など）= 直す候補
- 美術ボード（画風・色味・時代設定）との齟齬 = 直す候補
- 欄外/画面内の指示書き・補助線の識別（無視対象）

## 6. 比率・解像度の事前調整（実装済み）

`src/genzu_fix/image_aspect.py`

- 原図の縮尺・比率は変えない。**余白(パディング)**で許容比率へ寄せる。
- 許容比率の中から**余白が最小**になる比率を自動選択。
- 復元は**相対座標(割合)**で行い、出力比が要求比とズレても正しく切り戻す。
- 例: 原図 2000×1391 → `3:2` を選択、左右に43px余白 → 生成 → 余白除去で元画角へ。

## 7. 運用前提

- 1カット約5分（プロンプト3分＋生成2分）想定、300カットで約25時間 → 夜間バッチ。
- カット単位で中間成果物をファイルに残し、**途中再開可能**にする。
- リテイクは最大3回（充足判定はAI、後段で実装）。失敗カットは人手送りフラグ。

## 8. 実行経路 / ネットワークの知見（重要・検証済み）

Higgsfield への接続経路を切り分けた結果:

- **MCPコネクター経由の操作は許可リスト不要で動く**（Anthropicサーバー経由）。
  実際に通った: `balance`(残高), `models_explore`(仕様), `media_upload`(presigned URL発行),
  `generate_image`(命令発行), Google Drive からの原図ダウンロード。
- **サンドボックスから直接出る通信は環境の egress 許可リストで制限される**。
  → 以前は `upload.higgsfield.ai` への PUT が `403` でブロックされていた。
  → **【2026-06-19 解消・実証】** 環境の Network access に Higgsfield を許可後、
     `upload.higgsfield.ai` への直 PUT が **200** で成功。`media_upload`→PUT→`media_confirm`→
     `generate_image`→`show_generations`(結果URL取得)→ダウンロード→切り戻し までの
     **全工程を web セッション内で完結**できることを確認済み。
- `media_import_url`（サーバー側fetch）は **Google Drive 各URL形式でリダイレクト超過(>2)で失敗**。
  Drive は import 非対応とみなす。直リンク配信のホストが必要。
  → ただし `media_upload`+PUT が web 上で通るようになったため、import_url は不要になった。

### 結論（実行方針）
- **本番の300カットバッチ**: ユーザー環境（Mac/サーバー）で Python＋**Higgsfield CLI** 実行。
  egress 制限が無いため `media_upload`+PUT もCLIも素直に通る。これが本筋。
- **Claude Code on the web で動かす場合**: 環境の Network access に Higgsfield を許可
  （`*.higgsfield.ai`, `upload.higgsfield.ai`, `*.cloudfront.net` ＋既定リスト、または Full）。
  → **2026-06-19 に許可済み・動作実証。web 上でお試し〜小バッチが完結可能。**

## 8.5 実測結果（2026-06-19 / shz_02_143 南康郡_街・一点透視・1枚入力）

原図 `shz_02_143_genzu_kari.png`（2104×1464）を GPT Image 2（high / 2k / 3:2）で編集:

- **配置維持 ◎**: 左右の家並みの後退、中央消失点上の二層楼門、正面の通り — 構図・前後関係を保持。
- **指示・補助線の除去 ◎**: 欄外管理情報（作品名/カットNo/BG名/TIME/スタジオ名）・手書き指示
  `淡色処理`・パース補助線・VPマーク・地平線・四隅の黒バー・タップ穴を**完全に除去**。
- **パース修正 ◎**: 全エッジ/軒/地面線が中央消失点へクリーンに収束。手描きの揺れも整う。
- **比率パイプライン ◎**: 3:2/左右46px → 生成2048×1360 → 相対座標で元画角 2104×1464 に正確復元。
- コスト **7 credits/枚**（high・2k）。300カットでも実用圏。
- **留意点**: 出力がクリーンなベクター調線画になり、手描き美術原図のニュアンスは薄い。
  細部（看板・装飾）は再解釈される（配置は維持）。**画風の寄せは美術ボード入力（2枚目）が鍵** → 次に検証。
- プロンプト雛形は本リポジトリのコミット履歴（generate_image 呼び出し）に記録。

### 8.6 2枚入力（原図＋美術ボード）実測（2026-06-19 / 同カット）

ボード `SZ#2#3_南康群_街並_清書.png`（1721×1135）を IMAGE2 に追加して再生成:

- **2枚入力は可** ◎ — GPT Image 2(Higgsfield) は reference image を2枚受理。
- **役割分担が効く** ◎ — プロンプトで「IMAGE1=配置を守る原図／IMAGE2=時代・構成物の語彙のみ。
  色・光・アングルは流用しない」と指示 → 出力は**白黒線画・一点透視を維持**しつつ、
  ボードから組物・反り瓦・紅提灯・木格子・壁パネル・石畳などの**様式/構成物を反映**。
  1枚版より南康郡らしさが向上し、構成物が具体化。
- **方針確定**: 出力は**白黒線画**。ボードは**時代背景・構成物の参照**に使い、**色味は参照しない**。
- **保留**: 線のタッチ（クリーンなベクター線 vs 手描き感）は美術側と相談して決定。今回は寄せを固定せず。
- 記録: `runs/ledger.jsonl` に両ラン（1枚/2枚）を保存。台帳スキーマは `src/genzu_fix/ledger.py`。

### 永続化の注意
- 以前は git push が `403` で失敗していたが、**2026-06-19 に解消**（egress 許可と同タイミング）。
  ブランチへ直接 push 可能。Drive バックアップ（`genzu-fix_backup_*.md`）は予備として残す。

## 9. 未決事項 / 次アクション

- [x] 環境の Network access に Higgsfield を許可 → **web 上で生成完結を実証（2026-06-19）**
- [ ] git push の 403 を解決（リポジトリ書き込み権限の確認）
- [x] 香盤表の **.xlsx 原本**の所在を特定（Drive: `俺だけレベルアップな件_パチスロ_Aパート_BG進行表_20260121.xlsx`）
- [x] 原図＋プロンプトで「配置維持・指示無視・パース修正」を実測（1枚入力で全て◎）
- [x] **2枚入力（原図＋美術ボード）を実測** → 可。ボード=時代/構成物の参照、出力は白黒線画で確定（色は不参照）
- [ ] 線のタッチ（ベクター vs 手描き感）を美術側と相談して決定 ← 判断保留中
- [x] 生成台帳の可視化（**ダッシュボード**）: `dashboard.py` がカット番号順HTMLを生成（§12）
- [x] `export_visible_to_png` のテキストレイヤー除外（既定ON, `drop_text`）＝(a)
- [~] カット→シーン索引(b): 索引CSV様式＋ボード候補マッチャ実装。中身は香盤表/絵コンテ待ち（§13）
- [ ] 原図サンプル2〜3枚で原図理解v1を試行
- [ ] 「場所名→ボード」対応表のフォーマット確定
- [ ] 本番骨組み: CLI呼び出しラッパ / 香盤表パーサ / 中間成果物の保存規約
- [x] **生成結果を元PSDへ新規レイヤー差し込み**（`insert_result_layer`・枝番リテイク対応）を実証
- [ ] PSD→PNG→生成→切り戻し→**PSD差し込み**→台帳記録 を1コマンドに連結
- [ ] `export_visible_to_png` のテキスト/参考レイヤー除外ルール（実運用でパターン収集後）

## 10. PSD→PNG 書き出し（実装済み・2026-06-19）

`src/genzu_fix/psd_export.py`（`psd-tools` 使用）。手動の「表示レイヤー書き出し」を自動化。

- `export_visible_to_png(psd, out, bg=(255,255,255))` … **本命**。PSD に保存された
  合成プレビュー＝**表示レイヤーの WYSIWYG** をそのまま PNG 化（高速・忠実）。`bg=None` で透過維持。
- `list_layers(psd)` … レイヤー一覧（名前/可視/種別/bbox/階層）を取得。
  実 PSD で「絵」と「指示・補助/チェック」の分離構造が見えた（例: `用紙` `100Fr赤`(枠)
  `tap`(タップ) / `LO` 配下の `【演出】【監督】【作監】【総作監】` チェック群 / `線画` `ラフ`）。
  → 原図理解 v1 の素性データとして有用。
- `export_with_overrides(psd, out, show=, hide=)` … レイヤー名で可視を上書きして再合成
  （`.visible` 書き換え＋`composite(force=True)`。ネスト層にも効く）。
- `insert_result_layer(psd, image, out_psd, base_name="AI原図修正")` … **最終成果物**。
  生成結果(PNG)を元PSDの**最前面に新規レイヤーとして差し込み**、別PSDへ保存（入力は非破壊）。
  元レイヤー構成は全保持。**リテイクは枝番**で積む（`AI原図修正` → `AI原図修正_02` → `_03`…）。
  検証済み: 保存→再オープンでレイヤー存在・元レイヤー保持・合成は最新を最前面に反映。

### 生成結果のPSD差し込み（insert_result_layer）の要点
- psd-tools 1.17 は書き込み可能: `PixelLayer.frompil(img, parent, name=, top=, left=)`＋`PSDImage.save()`。
- **日本語レイヤー名**: PSD レガシー名は macroman で書くため日本語は不可。
  `frompil(name=...)` はレガシー名へ直書きして save 時に `UnicodeEncodeError`。
  → 回避: frompil 後に `layer.name = "AI原図修正"` と**セッター経由**で代入すると、
  レガシー名は `"?"`、Unicode 名は `luni` タグブロックへ格納され、**Photoshop は luni を表示**。
- frompil は parent 末尾＝最前面に追加。差し込み層はフルキャンバス不透明なので最前面が見える。
- 入力PSDは変更せず必ず `out_psd` に保存（非破壊）。本番では出力を別名/別ディレクトリに。

### 運用で詰める予定（パターン収集後に判断）
- `export_visible_to_png` で**引継ぎメモ等のテキストレイヤー/参考資料が一緒に写る**ケースがありうる。
  まず実運用でパターンを集め、除外ルール（`kind=='type'` の除外や名前規則）を後で決める。
  `list_layers` が `kind` を返すのでテキスト層の洗い出しには使える。

### 検証で分かった psd-tools の注意点
- `composite()` は既定で**保存済みプレビューを返す**（可視変更が反映されない）。
  上書きを効かせるには `composite(force=True)` で再レンダリングが必須。
- `composite(layer_filter=...)` は**トップレベルのレイヤーにしか呼ばれない**（ネスト不可）。
  そのため名前指定の on/off は layer_filter ではなく `.visible` 書き換え＋`force=True` で行う。
- 「枠・タイトル・タップ」等が **背景ラスター(`LO用紙`)に焼き込まれている**ことがあり、
  名前レイヤーを隠しても完全には消えない。完全な「絵だけ抽出」は後段の原図理解で扱う。

## 11. CLI（実装済み・2026-06-19）

`src/genzu_fix/cli.py`（`python -m genzu_fix.cli`、PYTHONPATH=src）。
生成だけ環境で認証経路が違うため **prep / finish の2フェーズ**に分割。

- `prep <psd> [--prompt-file f] [--board img ...] [--out-dir d] [--transparent]`
  → 表示合成PNG → 比率パディング → `manifest.json`（psd/visible/padded/boards/prompt/prep）。
- （生成）padded.png（＋boards）を GPT Image 2 へ。web では MCP、本番は Higgsfield CLI/API。
- `finish <manifest> --result gen.png [--out-psd o] [--base-name AI原図修正]
   [--job-id ..] [--result-url ..] [--cost ..]`
  → 余白切り戻し → 元PSDへレイヤー差し込み → `runs/ledger.jsonl` に記録。

通しテスト済み（test.psd）: prep→(疑似生成)→finish で `AI原図修正` 層が入り台帳1行追加、元レイヤー保持。
残: 本番環境向けに生成バックエンド（Higgsfield CLI/API）を `run` サブコマンドへ統合。

## 12. 作業ダッシュボード（実装済み・2026-06-22）

`src/genzu_fix/dashboard.py`。`runs/ledger.jsonl`（生成結果）と任意の「カット一覧JSON」を
突き合わせ、**カット番号順**に〔原図 / 美術ボード / プロンプト / 生成結果サムネ / 状態 / 更新〕を
並べた**自己完結HTML**（外部依存なし）を出力。

- 状態: 未生成 / 生成済 / リテイクN回（同一カットの複数ランを集約、最新を先頭表示）。
- カット一覧JSONを渡すと**未生成カットも行**として出る（全カットの進捗が一望）。
- 生成: `python -m genzu_fix.dashboard out.html --ledger runs/ledger.jsonl --cuts cuts.json`
- 実データ確認済み（ep07の16カット＋生成済1カットで描画、番号順ソートOK）。

## 13. 名寄せ（カット↔ボード）の道具（実装済みの範囲・2026-06-22）

`src/genzu_fix/naming.py`
- `parse_board` / `build_board_index`: ボード名 → シーン/場所/時間/天気/改訂に構造化。
- `parse_cut_codes`: 原図名 → ep＋カット番号群（複数束・枝番対応）。
- `match_board(query, index)`: {scene,place,time,weather} に近いボード候補を上位順に提案
  （シーン一致+3/場所重なり+2/時間+1/天気+1、表記ゆれ郡⇄群等を吸収）。
- `runs/cut_index.example.csv`: 「カット→シーン/場所/時間/天気」索引の様式（香盤表/絵コンテから半手動で埋める）。

未確定: 索引の中身そのもの（cut→scene/place/time）は香盤表・絵コンテ由来。
これが埋まれば prep が自動でボードを引き、ダッシュボードのボード列も自動になる。

## 14. 重要な前提の修正（2026-06-22・レビュー指摘より）

サンプル生成のレビューで判明した、設計の前提に関わる指摘。**未解決のまま本番に進まないこと。**

### 14.1 「主体に属する/環境に属する」の線引き（QCの no_subject 定義）
- 誤: 「固定物は残す／可動物は消す」。可動か固定かは無関係。
- 正: **主体（キャラ）に属するものを消す**（持つ/身につける/連れている物・人物由来の影/効果）。
  **環境に属するものは残す**（棚の本は残す／人物が持つ本は消す）。
- 判断には**絵コンテ・美術ボードの文脈が要る**ことが多い。作品ごとに美術側と定義する。

### 14.2 美術ボード・絵コンテをプロンプトに使えていない（現状の穴）
- ep7サンプルの生成は**ボード未参照・絵コンテ未参照の“当て推量”**だった。
- 本来 §3 の通り、①原図理解 ②プロンプト統合 で **絵コンテ（その場面が何か）＋美術ボード（時代/様式/構成）**
  を読み込むべき。ここが未接続。ep7はボード未確定・絵コンテが92MB(未取得)で、まだ素材も揃っていない。

### 14.3 キャラ遮蔽カットの「背景補完」は捏造になる（手法の限界）
- キャラが画面下を覆う構図（膝上など）では、**隠れた地面/背景は原図に描かれていない**。
- そこから人物を消して生成補完すると、**AIが背景を捏造**する（根拠なし）。例: 233-④の地面・下草。
- 正しい運用:
  - **BGonly素材を使う**（スタジオは `*_BGonly` を作っている＝背景は別に用意される前提）。
  - BGonlyが無ければ、隠れた背景は**絵コンテ＋美術ボードに基づき意図的に設計**する領域であり、
    インペイントで埋める対象ではない。
- → **本ツールの「人物除去インペイント」はキャラ遮蔽カットには不適**。
  適用対象は「背景が主役のカット」「BGonly素材があるカット」に絞る。カット選別の判定基準に反映する。

## 15. 探索で分かったこと（2026-06-22・続き）— コンテ/注記の活用と方針の再修正

### 15.1 大容量ファイルは「リンク共有」でサンドボックス直取得・直読みできる（実証）
- 原図フォルダを共有済みのため、`gdrive.py` で **絵コンテ92MB を直接DL**でき、`PyMuPDF` で
  任意ページをレンダリングして**コンテの中身を読める**（カット番号は手書き＝画像、テキスト層は無い）。
- → 「コンテ/ボードから情報を得る」は web セッションでも**実際に可能**。大判PSDも同様に取得可。

### 15.2 §14.3 の「キャラ遮蔽カットは対象外」は行き過ぎ。撤回する
- 正しい姿勢: カットを選別して逃げない。人物は消すが**アイレベル・空間・プレーン構造を保てば**解ける。
- 重要: **原図の注記がプレーン(BOOK)構造と撮影意図を教えてくれる**。
  例 cut233(C.233): 立ち人物＋森。画面下は地面ではなく**「手前のモヤ BOOK1」＝別の前景霧プレーン**。
  右隅に**回転/ぼかしの撮影メモ＋円形図**（コンテ233の「DF:ぼかし/速度線」と一致）。
  → BG原図として正しいのは「**森プレーンを描き、下部は霧BOOK用に開けておく**／人物・付随物は消す」。
  　以前の④が描いた“草地”は誤り（そこは地面でなく霧プレーン領域）。
- 学び: 単純インペイントでなく、**注記＝プレーン分け・撮影指示・アイレベルを読んで反映**すれば
  キャラ遮蔽カットも背景の叩き台にできる。情報（コンテ/ボード/注記）が増えるほど精度が上がる。

### 15.3 名寄せの実地確認
- genzu のヘッダーは `C.233` 等、**カット番号が明記**されている（欄外管理情報）。
  コンテの該当カットと突合でき、番号対応自体は取れる（中身は「BG」と「効果/芝居」で見え方が違う点に注意）。

## 16. 品質バーの指摘（2026-06-22・ユーザーレビュー）— 線質と「品位の引き上げ」

サンプル出力への重要な批評。**ここを満たさないと“使える原図”にならない。**

### 16.1 線質（最優先課題）
- 問題: 出力が**一定太さのクリーンな輪郭＝塗り絵/ベクター調**で、**入り抜き（線の入り・抜き・強弱）**や
  手描きの揺らぎが無い。「AI絵」感が強い。
- 要件: 熟練美術の鉛筆クリーンアップのように、**近景/輪郭は太め・遠景/内部は細く繊細・植生や質感は
  軽い擦れ/破線**で、**線幅変調と入り抜き**を持たせる。均一輪郭・CG的な無機質さを避ける。
- 注意: GPT Image 2 は放置すると均一ベクター線に寄る。**線質は言葉で明示強制**しないと出ない
  （プロンプトに「hand-drawn / line-weight variation / 入り抜き / not vector / not coloring-book」）。
  それでも限界がある場合は、線画特化モデルや後処理(線幅変調)も検討。

### 16.2 「転写」でなく「品位の引き上げ」
- 問題: 元原図の低品質をそのまま残し、人物/記号を消す“整理作業”に留まっている。
- 要件: パースの狂い・弱い構造を**直して絵として成立させる**。様式的に正しい構造ディテールを
  控えめに足す。＝「背景だけの絵を作る」のではなく**作画品質を上げる**のが目的。

### 16.3 プロンプトはシーン固有に厚く書く（ユーザー実例が有効）
- ユーザーの良好な結果は、**具体的なシーン記述**で出ていた: 構図(例「2-1-2構成・5棟」)を名指しで
  維持、舞台設定(中国南北朝・山岳集落・国境地帯)、各建物を格上げしない指示、使ってよい/避ける
  植生・生活道具の列挙、雨設定の表現方法、出力=整理線画。
- → §13 の「作品共通＋シーン固有＋カット固有」テンプレに、**構造の名指し維持**と**線質指定**を
  必須項目として組み込む。アイレベルだけでなく §15.2 の項目群＋線質をワンセットで。

## 17. 訂正（2026-06-22）— カット番号の根拠と3層の取り違え

### 17.1 カット番号は「ファイル名」で確定する
- 例: `shz_01_049_genzu` = 1話 cut 049。**ファイル名のカット番号を正とする**。
- 原図ヘッダーの手書きカット番号（朱書き）は**誤読・不一致がありうる**（C.49 を C.45 と読む等）。
  → 手書きは参考に留め、ファイル名と食い違う場合は**フラグして人手確認**。名寄せの注意点。

### 17.2 構図(2-1-2 等)は「カット固有」。シーン/作品共通ではない
- 誤: 「2-1-2構成・5棟」をシーン固有プロファイルに入れる。
- 正: **構図・アイレベル・カメラ・画角・見切れ・プレーン構成・雨の付き方は“カット固有”**。
  原図＋コンテから**カットごとに読み取る（原図理解）**。
- 共有できるのは「場所の正体（どのボード・その場所の建築語彙）＝シーン固有」「時代/様式/NG/線質/出力＝作品共通」まで。
- §16.3 で「構造の名指し維持を必須フィールド化」と書いたが、その値は**カット固有フィールド**として
  毎カット生成する（テンプレに固定値で書くものではない）。scene_info もこの粒度で持つ。

## 18. 重大バグ: 生成結果が原図フレームとレジストしない（2026-06-23）

### 症状（ユーザー指摘）
- AI原図修正レイヤーが元原図/フレーム位置と合わず、**だいたい上方向にずれる**。これでは使えない。

### 切り分け（実測）
- 解像度の数値バグ**ではない**: PSD canvas = visible PNG = restored = 同寸、(0,0)等倍配置で確認。
- 真因: **原図シート全体（上部の管理ヘッダー＋余白＋撮影フレーム＋絵）をモデルに渡している**ため、
  モデルが「絵」をシート全面に描き直し、**ヘッダー帯の分だけ絵が上に詰まる**。
  実測(233): 撮影フレーム上端が y=281 = シート上から **19.1%**。この帯にも絵が描かれて上方シフト。
- 南康郡(shz_02_143)で目立たなかったのは、絵がシートいっぱい＝ヘッダー余白が小さかったため。

### 修正: フレーム基準パイプライン（`src/genzu_fix/frame.py`）
- `detect_frame`: 行/列の暗さ投影のピークから**撮影フレーム矩形**を推定（ヘッダー/タップを除外）。
- `crop_to_frame`: フレーム内だけを生成入力にする。
- `paste_into_frame`: 生成結果を**同じフレーム矩形へ正確に戻す**（フレーム外＝ヘッダー/余白は維持）。
- これで AI レイヤーが元原図とレジストする。比率パディング(§6)はフレーム切り出し後の小さな画に適用。
- TODO: prep/cli にフレーム工程を組み込み、全カット再処理。フレーム検出の頑健性も要追試
  （枠が薄い/絵が枠に被る原図でのフォールバック）。

## 18.1 訂正: フレーム切り出しは「撮影フレーム」で切ってはいけない（2026-06-23）

§18 の `detect_frame`＋crop は**内側の矩形を掴み、移動用の余分（作画範囲）を切り落としていた**。誤り。

### データ根拠（cut233, 検出枠 l,t,r,b=83,281,1995,1431 / 画像2082x1473）
枠の**外側**に残るインク率: 上8%(=ヘッダー文字), **下91%**(=前景霧/絵), **左19% / 右21%**(=作画)。
→ 左右・下は普通に絵が描いてある＝**切ると作画の余分を失う**。

### 背景知識（重要）
原図はカメラ移動（PAN/TU/TB/SL）を見込み、**撮影フレームより外側に“余分”を描いてある**。
実際に 228/233 の原図注記に「〜SLするのでフレーム外余分に作成してください」。
→ **撮影フレームで切るのは厳禁**。余分は成果物に必要。

### 正しい方針
- **除去対象は非絵チャート部のみ**: 上部の管理ヘッダー帯（作品名/C/TIME/スタジオ名）＋タップ穴＋真の白余白。
- **作画範囲（撮影フレーム＋余分）は全部保持**して生成・レジストする。
- **撮影フレーム矩形は「切る境界」ではなく、映る範囲/PAN参照のメタ情報として記録**するだけ。
- 実装やり直し: detect は「ヘッダー帯の下端」を求める方向に。可能なら原図の標準フレーム規格や
  PSD側のフレーム情報を使う（画像ヒューリスティックより堅い）。本処理前に数枚で目視検証する。

## 19. データ品質の前段問題（2026-06-23・ユーザー指摘）→ 絵コンテ/レイヤー選択が前段に必要

ep07 10カットの再処理で判明。生成品質以前に**入力の素性**で詰まる。

### 19.1 そもそも背景修正に不適なカットがある
- 薄すぎるラフ（272）、キャラ作画が主役のカット（194/258_265/295B＝顔アップ）。
- → これらは「背景原図修正」の対象外。**事前にカット選別（BG主役 / キャラ作画 / 不適）が要る**。

### 19.2 取得した“表示レイヤー”が背景原図の線とは限らない
- ep07 genzu PSD のレイヤーは `LO.jpg / B1_A2.jpg / セル参考 / モヤBOOK` 等。
- `export_visible_to_png` は**可視状態をそのまま合成するだけ**なので、キャラのセル/LO込みの絵を拾うことがある
  （194/258_265/295B が「キャラ主役」になった理由）。＝**純粋なBG原図の線レイヤーを選べていない**。
- → 「どのレイヤーが背景原図か」を選ぶ工程が必要（名前規則／kind／レイヤー構造の理解）。

### 19.3 結論
- §3 の「① 原図理解（香盤表・絵コンテ・原図注記）」が**やはり前段に必須**。
  - 絵コンテ → カットが背景主役かキャラ作画か／場面が何か を判定 → 不適カットは人手送り。
  - 正しいBGレイヤー選択・何を描くべきかの根拠。
- 次アクション候補: (a) カット選別の判定（コンテ＋レイヤー構造）、(b) BGレイヤー選択ロジック、
  (c) これらを prep の前に差し込む。

## 20. 位置ずれ（ピクセルシフト）の調整ロジック明文化と要因分解（2026-06-23）

### 20.1 座標変換パイプライン（fix2 現行）
記号: 原図シート (W0,H0)。各工程と座標。
1. PSD→可視PNG: composite。サイズ=(W0,H0)。**変換なし**。
2. ヘッダー除去 `strip_header`: 上端 y_h を検出し region=(0,y_h,W0,H0) で crop。body=(W0, H0-y_h)。**厳密**。
3. 比率パディング `build_input_image(body)`: body を最近傍許容比率へ。canvas=(Wc,Hc)、body を (px,py) に貼る。
   prep.frac = body の canvas 内割合。**厳密だが余白(バー)が入る**。
4. 生成(GPT Image): padded を入力、出力 (Wg,Hg)。**モデルは描き直す＝画素位置を保存しない**。
   さらにプロンプトで「fill the frame edge to edge」と指示＝**余白バーを保持しない**。
5. 切り戻し `restore_output_image`: 出力を prep.frac で crop し body サイズ (W0,H0-y_h) へ resize。
6. `paste_into_region`: body 結果を region=(0,y_h,W0,H0) に貼る。canvas=(W0,H0)。**厳密**。
7. PSD差し込み: (W0,H0) を PSD canvas(=同寸)へ。**リサイズ無し＝厳密**。

### 20.2 シフト要因の分解（2つ）
- **要因(i) 生成の描き直しは画素一致でない（支配的・原理的）**
  GPT Image は入力をトレースせず再描画する。特徴(木/建物/地面線)が微妙に移動・伸縮する。
  実測: 原図 vs AI結果のエッジ位相相関がカット間でバラバラ（dx ±40%超も）＝**画素相関が成立しない**
  ＝ピクセル一致の前提が無い。→ 幾何補正では消せない。叩き台では許容だが、精度要求時は
  (a) 画素忠実モード(img2img低強度/構造ロック/ControlNet系)、(b) 後段レジスト(特徴整合ワープ)、
  (c) 許容、のいずれか。
- **要因(ii) 「edge-to-edge充填」指示と「frac切り戻し」の不整合（系統誤差・小・修正可能）**
  工程4で「バー無しで充填」させる一方、工程5は「バー有り前提」で frac crop する＝**矛盾**。
  ヘッダー除去後の body は比率が許容比に近く padding は小さい(数%)ため誤差も小さいが、
  系統的な上下方向の微シフト/微スケールを生む。→ **convention統一で除去**:
  - 案A: パディング廃止。body をそのまま生成し、出力を body サイズへ resize して戻す（frac廃止）。
  - 案B: パディング維持＋「バーを保持」指示＋frac切り戻し（どちらか一方に統一）。

### 20.3 精度確認の自動化（現状は人手）
- 20.2 の位相相関＋既存QC(人物/付随物/白黒/画角)を組み合わせ、
  「相関の信頼度」「人物残り」等で **要確認フラグ** を自動付与 → 人手確認を減らす。
- 注: 要因(i)が大きいカットは相関が低く出る＝そもそも画素一致を期待しない方針かを先に決める。

### 20.4 A1: 系統誤差除去の規約（2026-06-23）
- 矛盾の解消: 生成プロンプトを「枠いっぱい充填(fill edge to edge)」から
  **「入力の枠・位置・スケールを厳密維持／余白は余白のまま・絵を広げない」**へ変更。
  これで「余白パディング＋frac切り戻し」と整合し、系統的な上下シフト/微スケールを消す。
- 残るずれは要因(i)=生成の描き直し由来のみになる（A2で方針判断）。
- 原因精査用に各カットの中間生成物4点を出す:
  ①原図PNG ②余白追加・解像度調整済みの参照入力 ③GPT Image 2 生成結果(raw) ④原図へ合わせ調整した結果。
