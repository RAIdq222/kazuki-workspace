# TODO — 原図修正自動化（尚善 ep7）

最終更新: 2026-06-25 / branch: claude/stoic-hopper-bezfdl

リモートのコンテナは使い捨て・work/ は来ない前提。状態は git に乗せて引き継ぐ。

---

## ✅ 完了（土台）
- **パイプライン**: レジストずれ解決（入力=出力グリッド §20.6）。原図は切らない既定。
  レイヤー選択 BG→LO→背景、visible/Base 切替。Higgsfield CLI 生成→PSDへ「AI原図修正」差し込み。
- **コンソール(server.py)**: 作品▸話数の2層タブ、カード一覧、プロンプト編集、ボード選択、
  OK/要修正、原図取り直しモーダル(visible/Base)、生成進捗バー、
  **生成前後の比較（横並び⇄スライダー）/ 左右入替 / 画像拡大**、**ボードのオンデマンド表示（ボタン/ホバー）**。
- **起動**: run_console.bat / run_console.py（PYTHONPATH不要・依存チェック付き）。
- **テスト**: tests/console_e2e.py（Playwrightで17項目・実ブラウザ検証）。
- **受け渡し**: scripts/gather_handoff_ep7.py + runs/handoff_DATA_README.md。

---

## 🎯 A. コンソールを「全カット完走」できる作業台にする（最優先）
ゴール: ダッシュボード上で全カットのAI生成を完了できる。
1. **複数選択＋一括/並列生成**（max ~8 同時）・キュー・完了通知。「未生成を全部回す」ボタン。
   → 300カットを夜間に流して朝チェック、を可能にする。今は1カットずつ。
2. **レイヤー手動オーバーライド**（025/052 の誤検出対策）。取り直しモーダルで
   レイヤー一覧から原図にするものを選べるように（list_layers は既にある）。
3. **リテイクに指示コメント**（なぜ要修正か）を添えて記録＋次プロンプトに反映。
   できれば画像に丸/矢印の簡易アノテーション。
4. **テイク履歴**（v1/v2/v3 を上書きせず保持・前のテイクに戻せる）。
5. **一覧の強化**: 状態/担当の視覚分離、状態・差し戻し回数でソート、キーボード操作(j/k/o/r/space)。

## 🎨 B. プロンプト/品質（prompt-design セッション主管・要データ受け渡し）
6. **カット原図理解の記入**（situation/remove）= 受け手が handoff/ep7 の画像を見て埋める。
   → 先に **C-1 のデータ push が必要**。
7. **ボード2枚入力**（IMAGE2=ボード画像）に進めるかの方針決定＆実装。
8. **品質の詰め**: 流背/イメージ背景の扱い、立体感・奥行き、太い幹が分割される問題 等。

## 📦 C. データ/運用（ユーザー操作が要る）
1. 作業マシンで `gather_handoff_ep7.py` を実行 → `handoff/ep7/` を commit/push（受け手が pull）。
2. **判断を一言で**: cut240 ボードの正（森 or 寝室）／出力 aspect の固定ルール／
   束カットは1ファイル=1生成でよいか／生成はどちらが回すか。
3. **GKV 23カット**の生成完走を確認・記録（ledger/サマリ）。
4. conte の命名規則（番号の入り方）を共有 → 必要なら gather のコンテ自動抽出を調整。

## 🧹 D. あると効く（後回し可）
- QCフラグ・コスト記録（生成回数/枚数）。
- ダーク背景トグル・サムネ拡大（線画の視認性）。
- パッケージ化（pip install -e .）して PYTHONPATH 問題を恒久解消。

---

### 次の一手の推奨
**A-1（一括生成）→ A-2（レイヤー手動選択）→ A-3（指示付きリテイク）** の順。
これでコンソールが「眺める道具」から「300カットを実際に捌く作業台」になる。
Bは C-1 のデータ push 待ちなので、並行してユーザー側で push を進めてもらうのが効率的。
